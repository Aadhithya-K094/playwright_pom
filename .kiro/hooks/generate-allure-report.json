{
  "version": "v1",
  "hooks": [
    {
      "name": "Generate Allure Report After Task",
      "trigger": "PostTaskExec",
      "description": "Generates Allure report after a spec task completes",
      "action": {
        "type": "command",
        "command": "npx allure generate allure-results --clean -o allure-report",
        "timeout": 60
      }
    }
  ]
}